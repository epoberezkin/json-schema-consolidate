var adapter = require('./index');

module.exports = adapter('is-my-json-valid', {
    // definitions: [ 'invalid definition' ],
    // maxLength: true,
    // minLength: true,
    // 'optional/zeroTerminatedFloats': true,
    // refRemote: true,
    // ref: [ 'remote ref, containing refs itself' ]    
});
